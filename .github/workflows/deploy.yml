name: Deploy to Strato Webhost
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: STRATO
    env:
      # Path under which the app is hosted, e.g. "/fitbod-to-hevy/". Set as a repository or
      # environment variable named BASE_HREF; defaults to the web root.
      BASE_HREF: ${{ vars.BASE_HREF || '/' }}
      OUTPUT_DIR: dist/fitbod-to-hevy-converter/browser
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests
        run: npx ng test --watch=false

      - name: Build Angular application
        run: npx ng build --configuration production --base-href "$BASE_HREF"

      - name: Add .htaccess for caching and compression
        run: |
          cat > "$OUTPUT_DIR/.htaccess" << EOL
          <IfModule mod_rewrite.c>
            RewriteEngine On
            RewriteBase ${BASE_HREF}

            # Serve index.html for any path that is not an existing file or directory
            RewriteCond %{REQUEST_FILENAME} !-f
            RewriteCond %{REQUEST_FILENAME} !-d
            RewriteRule ^(.*)$ index.html [L]
          </IfModule>

          <IfModule mod_expires.c>
            ExpiresActive On
            # Hashed bundles can be cached for a year; index.html must not be cached
            ExpiresByType text/css "access plus 1 year"
            ExpiresByType application/javascript "access plus 1 year"
            ExpiresByType image/x-icon "access plus 1 year"
            ExpiresByType text/html "access plus 0 seconds"
          </IfModule>

          <IfModule mod_deflate.c>
            AddOutputFilterByType DEFLATE text/html text/css application/javascript
          </IfModule>
          EOL

      - name: Deploy to Strato via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ftp.strato.com
          port: 21
          # Explicit TLS: plain FTP would send the credentials and the site in cleartext.
          protocol: ftps
          username: ${{ secrets.STRATO_FTP_USERNAME }}
          password: ${{ secrets.STRATO_FTP_PASSWORD }}
          local-dir: ./dist/fitbod-to-hevy-converter/browser/
          server-dir: ${{ secrets.STRATO_SERVER_PATH }}
          state-name: ".ftp-deploy-sync-state.json"
